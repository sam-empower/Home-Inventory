import { Icons } from "@/lib/icons";
import { useToast } from "@/hooks/use-toast";

export function ToastNotification() {
  const { toast } = useToast();

  return null; // Using the shadcn toast system, this is just a placeholder
}
